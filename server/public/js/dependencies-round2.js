//This file ensures that jQuery gets loaded before any other library files.
//This is necessary because so many of the libraries depend on jQuery.
define([
  'bootstrap.3.3.6'
], function(Bootstrap) {

});